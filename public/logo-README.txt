Save the TonTon Trailer Rentals logo image here as:

    logo.png

It is referenced by the app UI, the printable contract/addendum, and the emails
(as {APP_URL}/logo.png). Until this file exists the header falls back to text.
